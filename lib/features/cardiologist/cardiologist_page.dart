import 'package:flutter/material.dart';

class CardiologistPage extends StatelessWidget {
  const CardiologistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Cardiologist Page'),
      ),
    );
  }
}
